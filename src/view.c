#include <stdio.h>
#include "include/shm_utils.h"
#include "include/utils.h"
#include "include/view.h"

int main(int argc, char *const argv[]) {

    // shared_memory_t * shared_memory = get_shared_memory();

    // if (shared_memory == NULL) {
    //     return 1;
    // }


    // now we read from the shared_memory and we print the content
    // read_shared_memory()
    // printf("The content of the shared memory is: %s\n", shared_memory->buffer);

}
